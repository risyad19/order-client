$(document).ready(function(){
    $( "#start-date" ).datepicker({
        format: "YYYY-MM-DD"
    });
})

// ( function ( $ ) {
//     $( "#birthday" ).datepicker({
//      format: "yyyy-mm-dd"
//     });

// }) ( jQuery );
